@extends('admin.layouts.app')

@section('content')
    <div class="container">
        <div class="row justify-content-center">
            <div class="col-md-8">
                <div class="card">
                    <div class="card-header">{{ __('Register') }}</div>

                    <div class="card-body">
                        <form method="POST" action="{{ route('register') }}">
                            @csrf

                            <div class="row mb-3">
                                <label for="name" class="col-md-4 col-form-label text-md-end">{{ __('Name') }}</label>

                                <div class="col-md-6">
                                    <x-input name="name" type="text"
                                        class="form-control @error('name') is-invalid @enderror" :isRequired="true"
                                        attr="autocomplete='name' autofocus id='name'" :value="old('name')" />
                                </div>
                            </div>

                            <div class="row mb-3">
                                <label for="email"
                                    class="col-md-4 col-form-label text-md-end">{{ __('Email Address') }}</label>

                                <div class="col-md-6">
                                    <x-input name="email" type="email"
                                        class="form-control @error('email') is-invalid @enderror" :isRequired="true"
                                        attr="autocomplete='email' id='email'" :value="old('email')" />
                                </div>
                            </div>

                            <div class="row mb-3">
                                <label for="password"
                                    class="col-md-4 col-form-label text-md-end">{{ __('Password') }}</label>

                                <div class="col-md-6">
                                    <x-input name="password" type="password"
                                        class="form-control @error('password') is-invalid @enderror" :isRequired="true"
                                        attr="autocomplete='new-password' id='password'" />
                                </div>
                            </div>

                            <div class="row mb-3">
                                <label for="password-confirm"
                                    class="col-md-4 col-form-label text-md-end">{{ __('Confirm Password') }}</label>

                                <div class="col-md-6">
                                    <x-input name="password_confirmation" type="password" class="form-control"
                                        :isRequired="true" attr="autocomplete='new-password' id='password-confirm'" />
                                </div>
                            </div>

                            <div class="row mb-0">
                                <div class="col-md-6 offset-md-4">
                                    <button type="submit" class="btn btn-primary">
                                        {{ __('Register') }}
                                    </button>
                                </div>
                            </div>
                        </form>
                    </div>
                </div>
            </div>
        </div>
    </div>
@endsection
